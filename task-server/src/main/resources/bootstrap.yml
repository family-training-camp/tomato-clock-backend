spring:
  application:
    name: task-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        file-extension: yaml
        server-addr: localhost:8848
        namespace: 840a3150-57d2-43ed-81ea-be337878f9dc
        shared-dataids: common.yaml,task-server.yaml
        refreshable-dataids: common.yaml,task-server.yaml
server:
  port: 9000